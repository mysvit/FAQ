# Run interaction for debian bash.
# The -i means "run interactively", and -t means "allocate a pseudo-tty".
docker run -it debian /bin/bash

# Show processes in docker and last run 
docker ps -l

# removae container 
docker rm [container]
# remoove images
docker rmi [name]

